{
  "name": "fit-o-charity-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd backend && npx tsc --project tsconfig.json && cd ../frontend && npm run build",
    "deploy:railway": "echo 'Preparing for Railway deployment...'"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "24.x"
  }
}